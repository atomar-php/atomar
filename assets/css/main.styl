label {
    font-weight:600;
}
hr.dark {
    border-color:#ccc;
}

.growl {
    position:fixed;
    z-index: 9999;
    top: -1px;
    left: 0;
    right: 0;
    text-align: center;
    width: 100%;
    margin: 0 auto;

    .alert {
        border-radius:0;
        padding:2px 5px;
        font-size:13px;
        width:100%;
        margin:0;
    }

    .alert button {
        line-height:14px;
    }

}
.alert {
    margin-bottom: 5px;
}
.alert.fade.in {
    opacity: 0.95;
}
.validate-input {
    position:relative;
    z-index:1;
}
.validate-msg {
    position:relative;
    top: -5px;
    padding-top:3px;
    padding-bottom:1px;
    z-index:0;
    font-size:10px;
    text-align:center;
    line-height:16px;
    margin-bottom:0px;
}
.validate-msg.alert-success {
    border-color:rgb(176, 204, 152);
}
.validate-msg.alert-danger {
    border-color:rgb(216, 176, 182);
}
.validate-msg.alert-info {
    border-color:rgb(164, 208, 218);
}
.inline-edit-container {
    position:relative;
}
.inline-edit-form {
    border: solid 5px #fff;
    -webkit-box-shadow: 0px 0px 25px 7px rgba(0,0,0,0.18);
    -moz-box-shadow: 0px 0px 25px 7px rgba(0,0,0,0.18);
    box-shadow: 0px 0px 25px 7px rgba(0,0,0,0.18);
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
    position: absolute;
    margin: -3px;
    top:0;
    right:0;
    left:0;
    background-color:#fff;
}
.inline-edit-controls span {
    padding: 5px 5px 0 0;
    margin-left:5px;
}
.inline-edit-input {
    font-size:14px;
}
.link {
    color: #08c;
    cursor: hand;
    cursor: pointer;
}
.link:hover,
.link:active,
.link:focus {
    color: #005580;
    text-decoration:underline;
    outline: 0;
}
form.short {
    margin-bottom:0;
}
.tip, .pop {
    cursor:hand;
    cursor:pointer;
}
.page-header h2 {
    margin: 0px;
    font-size:35px;
    color:rgb(90,90,90);
    text-rendering: optimizelegibility;
    color: #333;
    font-family:verdana;
}
.body-small .wrap {
    width:300px;
}
.body-small .container {
    width:260px;
}
.body-medium .wrap {
    width:600px;
}
.body-medium .container {
    width:560px;
}
.pending-confirmation {
    background-color:#fcf8e3!important;
    color:#c09853!important;
}
tr.pending-confirmation td {
    background-color:#fcf8e3!important;
}
body ::-webkit-scrollbar {
    -webkit-appearance: none;
    width: 8px;
    height: 8px;
    background-color: rgba(0,0,0,.08);
}

body ::-webkit-scrollbar-thumb {
    border-radius: 4px;
    background-color: rgba(0,0,0,.2);
    -webkit-box-shadow: 0 0 1px rgba(255,255,255,.25);
}
body ::-webkit-scrollbar-thumb:hover {
    background-color:rgba(0,0,0,.25);

}
[data-lightbox] {
    cursor:pointer;
}
.lightbox .row [class*="span"]:first-child {
    padding-left: 0;
}
iframe.modal {
    border: 1px solid #999999;
    border: 1px solid rgba(0, 0, 0, 0.2);
    border-radius: 6px;
    -webkit-box-shadow: 0 3px 9px rgba(0, 0, 0, 0.5);
    box-shadow: 0 3px 9px rgba(0, 0, 0, 0.5);
    top:30px;
    position:absolute;
}
body.modal-open, .modal-open .navbar-fixed-top, .modal-open .navbar-fixed-bottom {
    margin-right:0px;
}
.lightbox .modal-dialog {
    width:auto;
    padding:0;
    display:none;
    margin:0;
    display:block;
}
.lightbox .modal-body {
    padding: 15px 30px 0 30px;
}
.lightbox .modal-content {
    border: none;
    border-radius: 0;
    box-shadow: none;
}
.lightbox-wrap {
    overflow-y:scroll;
    position:relative;
    top:0;
    bottom:0;
    right:0;
    left:0;
    z-index:1041;
}
.lightbox-wrap iframe {
    background-color:#fff;
}
.lightbox-wrap.on {
    position:fixed;
}
.body-as-lightbox {
    overflow:hidden;
}
.body-as-lightbox .container {
    padding:0px;
    width:auto;
}
.page-controls {
    margin-top: -15px;
}
.page-controls i {
    margin-top: 4px;
}
.page-controls a {
    color: #666;
}
h1 a.permalink,
h2 a.permalink,
h3 a.permalink,
h4 a.permalink,
h5 a.permalink,
h6 a.permalink {
    visibility:hidden;
    font-size:18px;
    padding:5px;
    font-weight:100;
    text-decoration:none;
}
h1:hover a.permalink,
h2:hover a.permalink,
h3:hover a.permalink,
h4:hover a.permalink,
h5:hover a.permalink,
h6:hover a.permalink {
    visibility:visible;
}
.section-header {
    font-weight: 600;
    color:#555;
    font-size: 12px;
    border-bottom: solid 1px #ddd;
    margin-top: 0px;
    padding-left: 5px;
}
blockquote.text-success {
    border-color: #D0E4C8;
    background-color: #f9f9f9;
}
blockquote.text-info {
    background-color: #f0f7fd;
    border-color: #d0e3f0;
}
blockquote.text-warning {
    background-color: #fefbed;
    border-color: #f1e7bc;
}
blockquote.text-danger {
    background-color: #fcf2f2;
    border-color: #dFb5b4;
}
.tooltip-inner {
    /*font-weight:100;*/
}
.badge-info {
    color: #31708f;
    background-color: #d9edf7;
}
.badge-danger {
    color: #a94442;
    background-color: #E4B6B6;
}
.badge-warning {
    color: #8a6d3b;
    background-color: #F8E374;
}
.badge-success {
    color: #3c763d;
    background-color: #dff0d8;
}
.admin-sidebar .nav li>a {
    display:block;
}
.admin-sidebar .nav li>a:hover, .admin-sidebar .nav li>a:focus, .admin-sidebar .nav li.active>a {
    padding-right: 19px;
    color: #2a6496;
    text-decoration: none;
    background-color: transparent;
    border-right: 1px solid #428bca;
}
.admin-sidebar .nav>li>a {
    display: block;
    font-size: 13px;
    font-weight: 500;
    padding: 4px 20px;
}
.panel.panel-overflow-visible {
    overflow:visible;
}
.panel-title.panel-title-block > a {
    display:block;
    cursor:pointer;
}
.chosen-container.chosen-container-multi > ul.chosen-choices > li.search-choice {
    margin-top: 1px;
    margin-bottom: 1px;
}
/* loader animation */

.loader-animation .glass {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0px;
    right: 0px;
    z-index: 12;
}
.loader-animation .spinner {
    position:absolute;
    top:50%;
    right:50%;
    z-index: 11;
}
.loader-animation .sheet {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0px;
    right: 0px;
    z-index: 10;
    background-color: rgba(255, 255, 255, 0.6);
}
.loader-animation.fixed .glass,
.loader-animation.fixed .spinner,
.loader-animation.fixed .sheet {
    position:fixed;
}
.loader-animation.fixed .glass {
    z-index:1022;
}
.loader-animation.fixed .spinner {
    z-index:1021;
}
.loader-animation.fixed .sheet {
    z-index:1020;
}
.loader-animation:not(.blocking) .glass,
.loader-animation:not(.blocking) .sheet {
    display:none;
}

.loader-animation .spinner .circle1 {
    background-color: rgba(0,0,0,0);
    border: 5px solid rgba(0,183,229,0.9);
    opacity: .9;
    border-right: 5px solid rgba(0,0,0,0);
    border-left: 5px solid rgba(0,0,0,0);
    border-radius: 50px;
    box-shadow: 0 0 35px #2187e7;
    width: 50px;
    height: 50px;
    margin: 0 auto;
    position:relative;
    right: -25px;
    top:-25px;
    -moz-animation: spinPulse 1s infinite ease-in-out;
    -webkit-animation: spinPulse 1s infinite linear;
}

.loader-animation .spinner .circle2 {
    background-color: rgba(0,0,0,0);
    border: 5px solid rgba(0,183,229,0.9);
    opacity: .9;
    border-left: 5px solid rgba(0,0,0,0);
    border-right: 5px solid rgba(0,0,0,0);
    border-radius: 50px;
    box-shadow: 0 0 15px #2187e7;
    width: 30px;
    height: 30px;
    margin: 0 auto;
    position: relative;
    top: -65px;
    right: -25px;
    -moz-animation: spinoffPulse 1s infinite linear;
    -webkit-animation: spinoffPulse 1s infinite linear;
}

@-moz-keyframes spinPulse {
    0% {
    -moz-transform: rotate(160deg);
    opacity: 0;
    box-shadow: 0 0 1px #2187e7;
    }

    50% {
    -moz-transform: rotate(145deg);
    opacity: 1;
    }

    100% {
    -moz-transform: rotate(-320deg);
    opacity: 0;
    }
}

@-moz-keyframes spinoffPulse {
    0% {
    -moz-transform: rotate(0deg);
    }

    100% {
    -moz-transform: rotate(360deg);
    }
}

@-webkit-keyframes spinPulse {
    0% {
    -webkit-transform: rotate(160deg);
    opacity: 0;
    box-shadow: 0 0 1px #2187e7;
    }

    50% {
    -webkit-transform: rotate(145deg);
    opacity: 1;
    }

    100% {
    -webkit-transform: rotate(-320deg);
    opacity: 0;
    }
}

@-webkit-keyframes spinoffPulse {
    0% {
    -webkit-transform: rotate(0deg);
    }

    100% {
    -webkit-transform: rotate(360deg);
    }
}

/** Utility styles **/
.hover-visible {
    visibility:hidden;
}
.hover:hover .hover-visible {
    visibility:inherit;
}